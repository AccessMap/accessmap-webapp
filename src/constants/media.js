// Internally used responsive media breakpoints

const MOBILE_MIN_WIDTH = 360;
const TABLET_MIN_WIDTH = 824;
const DESKTOP_MIN_WIDTH = 1024;

export { MOBILE_MIN_WIDTH, TABLET_MIN_WIDTH, DESKTOP_MIN_WIDTH };
