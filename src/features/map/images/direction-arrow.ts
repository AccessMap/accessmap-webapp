import directionArrowUrl from "images/direction-arrow.png";

const directionArrow = new Image();
directionArrow.src = directionArrowUrl;
directionArrow.height = 48;
directionArrow.width = 24;

export default directionArrow;
